<?xml version="1.0" encoding="utf-8"?>
<!--
  Message bubble shape. One drawable serves both sides: the tint (peer vs. mine) and the
  bubble text colour are chosen in MainActivity.addBubble().
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/brand_surface_variant" />
    <corners android:radius="@dimen/bubble_corner_radius" />
</shape>

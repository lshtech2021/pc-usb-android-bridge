<?xml version="1.0" encoding="utf-8"?>
<!--
  Background for every programmatic Button (wired up via android:buttonStyle).
  Material themes only set materialButtonStyle, which XML-inflated MaterialButtons
  read; a Button created in Kotlin keeps the legacy flat grey platform background.
  Colours come from the theme roles so light/dark both stay legible.
-->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="?attr/colorSurfaceVariant" />
            <corners android:radius="@dimen/button_corner_radius" />
        </shape>
    </item>
</ripple>
